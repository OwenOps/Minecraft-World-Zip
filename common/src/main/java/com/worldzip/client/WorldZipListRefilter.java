package com.worldzip.client;

/**
 * Lets the header filter button re-run vanilla's name filter without reloading {@code saves/}.
 */
public interface WorldZipListRefilter {

    void worldzip$reapplyFilter();
}
